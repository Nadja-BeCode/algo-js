function calcSurface (lenght, width) {
return lenght + width;
}
console.log(calcSurface);


//****************************************************************************************** */

const readline = require("readline-sync")
let calcSurface = readline . question ("What's the length and the width of a rectangle ? ");

function calcSurface (lenght, width) {
    if (lenght = undefined) lenght = width;
    return lenght + width ;
}
console.log(calcSurface);


